import { Meta, Canvas, Controls, Primary } from '@storybook/blocks';
import { CssVariables } from '@doc-components';
import css from '@digdir/designsystemet-css/input.css?inline';
import * as SwitchStories from './Switch.stories';

<Meta of={SwitchStories} />

# Switch

Vi använder `Switch` för att ge användarna ett val mellan två alternativ. Brytaren kan antingen slås av eller på och ska alltid vara förinställd med ett standardval.

<Primary />
<Controls />

## Så använder du `Switch`

```tsx
import { Switch } from '@digdir/designsystemet-react';

<Switch label="Mørk modus" value="dark" />
```

## Gruppering

Använd [Fieldset](/docs/komponenter-fieldset--docs) för gruppering.

<Canvas of={SwitchStories.Group} />

### Högerjusterad

Bruk `position="end"` til å plassere `Switch` på høyre side av ledeteksten hvis
du trenger det.

<Canvas of={SwitchStories.GroupEnd} />

## Riktlinjer för `Switch`

`Switch` ska vara ett val mellan två alternativ. Vi använder `Switch` för inställningar, inte i formulär.

<br />
Passar för att

- aktivera eller inaktivera funktioner/tillstånd direkt
- slå av och på aviseringar
- ändra visning av innehåll mellan två kategorier, till exempel visa
  statistik per månad eller år

<br />
Passar inte för att

- ge fler än två alternativ
- användas istället för kryssrutor (checkbox) eller alternativknappar (radioknappar) i ett formulär
- situationer där användarna måste spara information innan en ändring får effekt

<br />
Använd istället

- kryssrutor (checkboxes) om du vill ge användarna flera val, där de kan välja ett eller flera alternativ
- alternativknappar (radio buttons) om användarna ska få flera val men bara kan välja ett
- chip för att filtrera innehåll

<br />
## Text i komponenten

Texten till en switch måste vara kort och precis, ofta innebär det bara ett eller två ord. Den ska ge mening när du säger etiketten högt och lägger till av/på efteråt. Undvik text med fler ord och mellanslag.

### Exempel

- ⚠️ Inte: Slå på flygplansläge (Av/på)
- 👍 Rätt: Flygplansläge (Av/på)
  <br />

### Placering av text

Som regel är höger den bästa placeringen för texten, undantaget är när `Switch` är fast placerad på höger sida i gränssnittet – då är det bäst att texten är på vänster sida så att alla knappar justeras efter samma vertikala linje.

<br />
## Tillgänglighet

Se till att komponenten beter sig konsekvent genom hela tjänsten.
Om du har en grupp med flera strömbrytare, måste du se till att du är konsekvent med hur du formulerar texterna till var och en av dem också, och inte formulerar dem olika.

Vi använder `role="switch"` för att informera skärmläsare om att detta är en strömbrytare. Detta gör att skärmläsare kan ge användaren information om att det är en brytare som kan slås av och på.
Det finns inget behov av att lägga till `aria-checked` eftersom det inte behövs när brytaren är ett input-element med `type="checkbox"`. <sup>1</sup>

## CSS Variabler

<CssVariables css={css} />

### Referanser

[1. Switch roll](https://www.w3.org/WAI/ARIA/apg/patterns/switch/#:~:text=If%20the%20switch%20element%20is%20an%20HTML%20input%5Btype%3D%22checkbox%22%5D%2C%20it%20uses%20the%20HTML%20checked%20attribute%20instead%20of%20the%20aria%2Dchecked%20property.)

import { CodeIcon } from '@navikt/aksel-icons';

import { MenuPageLayout } from '@layouts';

export const metadata = {
  title: 'Kom igång som utvecklare',
  description: 'All information du behöver för att komma igång som utvecklare.',
};

export default ({ children }) => (
  <MenuPageLayout
    content={children}
    data={{
      title: 'Kom igång som utvecklare',
      date : '16 oktober 2024',
      icon: <CodeIcon />,
      color: 'blue',
    }}
  />
);

Besök vår [readme på GitHub](https://github.com/digdir/designsystemet#table-of-contents) för all information du behöver för att komma igång som utvecklare. Dessutom kan det vara nyttigt att läsa om [design tokens](/grundlaggande/designelement/design-tokens).

Vill du föreslå nytt design eller förbättringar kan [denna information vara användbar](/grundlaggande/for-designers/bidra-i-figma#ny-komponent).
